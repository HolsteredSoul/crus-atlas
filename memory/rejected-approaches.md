---
name: rejected-approaches
description: Ledger of approaches we will NOT take, each with its why and revival condition — so fresh sessions don't resurrect them
metadata:
  type: project
---

Each entry: **what** — **why not** — **revive only if**. Check this before proposing an
approach. Nothing leaves this list by being re-proposed; only by its revival condition.

- **A journal / narrative session log** — rots into write-only noise nobody reads back;
  history is `git log`, state is the Status pointer, lessons are Slips, decisions are memory
  — revive only if those four prove insufficient to reorient a session.
- **Harness hooks to enforce the session ritual** — the user prefers convention-enforced
  discipline via AGENTS.md — revive only if the user asks, after the ritual has been skipped
  repeatedly.
- **Tube/ellipsoid procedural anatomy as the deliverable** — user inspected the leg and foot and rejected the result as unusable; basic naming and functional controls do not meet the visual/anatomical quality bar — revive only as an explicitly requested developer-only test fixture, never as the anatomical atlas shown for acceptance.
- **Guessing G ligament footprints from bounding boxes or filling the distal tibiofibular gap** — exposed source surfaces did not resolve anterior tibiotalar/IOL footprints; the spring candidate lacks a verified superomedial receiving surface — revive with source-registered attachment annotations or suitable registered anatomy and complementary references. Missing object names alone are not grounds for exclusion; inspect the actual surfaces.
