# Authored anatomy

No authored reconstruction passed this iteration's attachment-registration gate.
Keep future hand-authored `.blend` files here, separate from `../generated/`.
Each accepted reconstruction needs its own stable ID, source-specific attachment
annotations, two complementary inspectable anatomical references and explicit
`authored_reconstruction` provenance. Never overwrite authored assets during a
source rebuild. Current unaccepted spring-ligament source inspection is in
`../review/ligament-candidates.blend`; it is not part of the app GLB.
